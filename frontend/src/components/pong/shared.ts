import { ref } from 'vue';

export const p1_id = ref(0);
export const p2_id = ref(0);
export const p1_socket_id = ref('');
export const p2_socket_id = ref('');
export const username1 = ref<string>('');
export const username2 = ref<string>('');
export const match_id = ref(0);
export const socket_match_id = ref(0);